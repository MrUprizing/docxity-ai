{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install --legacy-peer-deps"

    },
  "deploy": {
    "preDeployCommand": ["cd ../../packages/database npm run db:migrate"],
    "startCommand": "npm run start"
    }
}
